# Evaluator Contracts

## Owned

- `EvalReport`

## Consumed

- `EnvSpec`
- `PolicySnapshot`
- `CheckpointManifest`

## 关键约束

- `EvalReport` 必须能关联到算法、backend、env、policy_version
- checkpoint 选择策略需显式记录
