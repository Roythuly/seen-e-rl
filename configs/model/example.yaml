encoder:
  kind: mlp
  hidden_sizes: [256, 256]
actor_head:
  kind: gaussian_policy
critic_head:
  kind: twin_q
feature_sharing:
  actor_critic_encoder: false
training_interface:
  forward_train: structured
notes: "PPO may use value head instead of twin_q, but keeps the same structured training interface."
